// NOTE: see https://github.com/nextflow-io/nextflow/issues/4578
notification {
    enabled = true
    to = "${params.emailTo}"
    from = "${params.emailFrom}"
    template = "template2.html"
    // binding = ...
    // OR
    // attributes = ...
}
