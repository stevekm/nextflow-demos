// custom notification settings

// NOTE: see https://github.com/nextflow-io/nextflow/issues/4578
// TODO: use embedded template string

notification {
    enabled = true
    to = "${params.emailTo}"
    from = "${params.emailFrom}"
    template = "templates/custom-notification-template.txt"
}
