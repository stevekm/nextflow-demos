profiles {
    docker {
        docker.enabled = true
        process.container = 'ubuntu:latest' // make sure to use a container with bash installed
    }
    test {
        process.debug = true
    }
}
